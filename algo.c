#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, char *argv[]) {
  float x = atof(argv[1]);
  float n = 40 + math.degree((sin(1/8 * x)));
  printf("%.2f\n", n);
  return 0;
}
